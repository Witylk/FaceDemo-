# --- 核心数据与图像处理 ---
numpy>=1.26.0
opencv-python>=4.10.0
Pillow>=10.0.0

# --- 关键底层依赖 (Windows 最难搞的部分) ---
# 警告：Windows 必须先装好 dlib 才能继续。
# 建议使用 whl 文件或 conda 安装，否则 pip 编译极易报错。
dlib

# --- 人脸识别模型数据 ---
# 这里直接指定版本，pip 会从官方源下载，不需要用户电脑有 Git
face_recognition_models>=0.3.0

# --- 人脸识别主程序 ---
# 它依赖上面的 dlib 和 face_recognition_models
face_recognition
